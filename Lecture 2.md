## Starting a Program 
# EGEC 450 // 08/31/2026

## 2. Steps to starting a program
This presentation targets starting a program on a general-purpose computer.
However, starting a program on an embedded system will be very similar and use the first three steps.
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
## 3. Building a project
This information adapted from Intro. to CCS and a Brief Review of C and Debugging, EGCP 450, Dr. John Faller, Cal State Fullerton.

When we build a project, all files are compiled, assembled, and then linked together.
For embedded system projects, a listing is typically generated.
A listing is a text file containing a mixture of the object code generated by the assembler together with the source code.
The address values shown in the listing are relative to the particular file being assembled.
After building the project, it can be loaded into memory to run and/or debug.
Typically you load and execute software out of RAM (e.g., DRAM or main memory) for a general-purpose computer.
But for an embedded system, we typically use nonvolatile flash ROM.
If debugging, the listing you see in the debugger will specify the absolute address showing you exactly where in memory your variables and instructions exist.
## 4. Compiler
4.1. Where are we now?
We will now examine each step to start a program:
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
4.2. The Compiler
Input: High-level language code
E.g., C, C++, Java, etc.
Output: Assembly language code
E.g., ARM, RISC-V, MIPS, Intel x86, etc.
Note: Output may contain pseudoinstructions
Instructions that the assembler understands, but are not in the machine's ISA (Instruction Set Architecture).
## 5. Assembler
5.1. Where are we now?
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
5.2. The Assembler
Reads and uses directives
Replaces pseudoinstructions
Produces machine language
Creates an object file
5.3. Assembler directives
Give directions to assembler, but does not produce machine instructions:
.text

.data

.globl sym:

.asciiz str:

.word w1,w2,...,wn:
5.4. Pseudoinstruction replacement
Some "higher-level" assembly instructions are not supported by the processor's ISA.
Instead, the assembler translates these pseudoinstructions into multiple simpler assembly instructions.
When debugging, compare what you wrote in assembly versus the assembly generated by the assembler.
You may see extra assembly instructions that you did not write due to this pseudoinstruction replacement.
5.5. Producing machine language
Arithmetic, logical, shifts, etc.
All necessary information is already within the instruction.
What about branches?
PC-relative
Once pseudoinstructions are replaced, we know by how many instructions to branch.
What about non-PC-relative branches and subroutine calls?
In ARM: B and BL
In RISC-V: jal and jalr
In MIPS: j and jal
In Intel x86: jmp and call
What about absolute or direct references to data?
E.g., in ARM: ADR R0, label
E.g., in RISC-V: la x8, label
E.g., in MIPS: la $s0, label
E.g., in Intel x86: MOV EAX, label
Branches and references to data?
Require full 32-bit addresses to instructions or data
Can't be determined yet
Must wait to see where code will appear in final program
Use two tables
Symbol table
Relocation table
5.6. Symbol and relocation tables
Symbol table
List of items in this file that may be used by this and other files
First pass: Record label-address pairs
Second pass: Produce machine code
Relocation table
Line numbers of items for this file which need the address completed/fixed later
Any label jumped to by a non-PC-relative branch or subroutine call (internal or external)
Any absolute or direct address to a piece of data
5.7. Object file format
Object file header
Text/code segment
Data segment
Relocation table
Symbol table
Debugging information
6. Linker or Link Editor
6.1. Where are we now?
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
6.2. The Linker or Link Editor
Combines several object (.o) files into a single executable ("linking").
Enables separate compilation of files/modules.
Changes to one file do not require recompilation of the whole program.
Think of editing/recompiling the source for Windows OS!
"Link Editor" name from editing the "links" in branch and link instructions.
6.3. Linker process
Step 1: Take text/code segment from each .o file and put them together.
Step 2: Take data segment from each .o file, put them together, and concatenate this onto the end of the text/code segments.
Step 3: Resolve references.
Go through relocation table and handle each entry using the symbol table.
Fill in the absolute address.
6.4. Output of linker
Executable file containing text/code and data (plus header).
May not have library object files resolved if dynamically loaded.
7. Loader
7.1. Where are we now?
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
7.2. The Loader
Executable files are stored on disk.
When one is to be run, loader must load it into memory and start it running.
Loader is the operating system (one of the OS tasks).
7.3. What does the Loader do?
Reads executable file's header to determine the size of text/code and data segments.
Creates new address space for program to hold the text and data segments, plus a stack segment.
Copies instructions and data from executable file into the new address space.
Copies program's arguments onto the stack.
Initializes machine registers.
Branches to start-up routine.
Copies program's arguments from stack to registers.
Sets the program counter (PC) (or instruction pointer (IP)).
7.4. Dynamic linking
Both the loader and linker are part of the operating system.
Modules can be linked and loaded at run time.
If a module is needed and already loaded, it need not be loaded again.
DLL: Dynamic-Link Library
8. Review: Steps to starting a program
For C program: foo.c
Compiler
Generates assembly program: foo.s
Assembler
Generates object (machine language module): foo.o
Linker
Links with other libraries such as lib.o
Generates executable (machine language program): a.out
Loader
Loads program into memory
